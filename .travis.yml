git:
  depth: 3
notifications:
  email: false

language: node_js
node_js:
  - "node"
os:
  - linux
  - osx

before_install:
  - gem install package_cloud
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get -qq update && sudo apt-get install -y libdbus-1-dev libglib2.0-dev; fi
  - npm install -g electron-builder
  - npm install -g electron-packager

install:
  - npm install
  - npm rebuild --runtime=electron --target=1.4.0 --disturl=https://atom.io/download/atom-shell --abi=48

script:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then npm run-script build; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then npm run-script buildOSX; fi

branches:
  only:
    - master